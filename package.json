{
  "name": "ds",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "private": true,
  "workspaces": {
    "packages": [
      "playgrounds/*",
      "packages/*"
    ],
    "nohoist": [
      "**/normalize-scss"
    ]
  },
  "npmClient": "yarn",
  "useWorkspaces": true,
  "stream": true,
  "scripts": {
    "build": "yarn lerna run build",
    "dev": "yarn lerna run dev"
  }
}
